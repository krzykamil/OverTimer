<% pending_audit_confirmations.each do |pending_audit_confirmation| %>
  <%= link_to "Confirming no overtime in week of #{pending_audit_confirmation.start_date}", root_path,
    class: 'btn btn-primary btn-block btn-lg', id: "confirm_#{pending_audit_confirmation.id}", data: { confirm: "You sure buddy?" } %>
<% end %>
